<?xml version="1.0" encoding="utf-8"?>
<!--
    FISIERUL PACKAGES.CONFIG - NuGet Package References

    CE ESTE ACEST FISIER?
    Contine lista tuturor pachetelor NuGet instalate in proiect
    NuGet este package manager-ul pentru .NET (ca npm pentru JavaScript)

    DE CE E IMPORTANT?
    1. Documenta ce biblioteci externe foloseste proiectul
    2. Permite restaurarea automata a pachetelor (nuget restore)
    3. Visual Studio il citeste pentru a sti ce pachete sa descarce

    CUM FUNCTIONEAZA NUGET?
    1. Specifici pachetul (id) si versiunea
    2. NuGet descarca DLL-urile in folderul packages/
    3. Le adauga ca Reference in proiect
    4. La build, DLL-urile se copiaza in bin/

    CUM SE FOLOSESTE?
    - In Visual Studio: Tools > NuGet Package Manager > Package Manager Console
    - Comanda: Install-Package EntityFramework -Version 6.4.4
    - Sau: Update-Package EntityFramework

    NOTA: In .NET Core/.NET 5+ se foloseste PackageReference in csproj
    Dar pentru .NET Framework, packages.config e metoda standard
-->
<packages>
  <!--
      PACHETUL ENTITY FRAMEWORK 6.4.4

      id="EntityFramework"
      - Identificatorul unic al pachetului pe nuget.org
      - Acesta e ORM-ul (Object-Relational Mapper) principal

      version="6.4.4"
      - Versiunea exacta instalata
      - 6.4.4 e ultima versiune stabila pentru EF6
      - NOTA: EF Core e diferit (EntityFrameworkCore), noi folosim EF6 clasic

      targetFramework="net472"
      - Pentru ce versiune .NET e instalat pachetul
      - net472 = .NET Framework 4.7.2
      - Trebuie sa coincida cu TargetFrameworkVersion din csproj

      CE CONTINE ENTITY FRAMEWORK?
      - DbContext, DbSet - clasele principale pentru acces la date
      - LINQ to Entities - query-uri LINQ pe baza de date
      - Code First, DB First, Model First - diferite abordari
      - Migrations - versionare schema DB (pentru Code First)
      - Lazy Loading, Eager Loading - strategii de incarcare date
  -->
  <package id="EntityFramework" version="6.4.4" targetFramework="net472" />
</packages>
